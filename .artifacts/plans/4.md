# Plan — Issue #4: Auto-deploy to Cloudflare Pages on push to main

- **Issue:** convergent-systems-co/branding-library#4
- **Domain:** ci
- **TL:** ci
- **Branch:** `feat/ci-auto-deploy`
- **Risk:** low
- **Type:** feature

## Goal

Phase 4 — eliminate manual `wrangler pages deploy` invocations. On every push to `main`, GitHub Actions must validate the library, build the converter outputs, build the Astro site, and deploy `web/dist` to Cloudflare Pages project `brand-atoms`.

## Acceptance criteria (from issue #4)

- [ ] New file `.github/workflows/deploy.yml` triggered on `push` to `main`.
- [ ] Workflow steps in order: checkout → pnpm/action-setup → actions/setup-node (v22) → `pnpm install --frozen-lockfile` → `pnpm validate` → `pnpm build` (root, converter) → `cd web && pnpm build` (astro) → `wrangler pages deploy web/dist --project-name brand-atoms --branch main`.
- [ ] Workflow uses GitHub secret `CSO_CF_TOKEN` (NOT `CLOUDFLARE_API_TOKEN`).
- [ ] Workflow aliases `env: CLOUDFLARE_API_TOKEN: ${{ secrets.CSO_CF_TOKEN }}` so wrangler/wrangler-action pick it up.
- [ ] Cloudflare account ID `e1fe0f0ce8ff18da4edc118372c30022` is set as a workflow-level env var (not a secret).
- [ ] If `CSO_CF_TOKEN` is unset, deploy step fails with a clear message referencing the settings URL.
- [ ] First successful run produces a new deployment at https://brand-atoms.com (validated post-merge by user).

## Non-goals

- Do NOT merge `validate.yml` and `deploy.yml`. They run independently.
- Do NOT wire converter outputs into the site (separate TL on issue #2).
- Do NOT merge the PR — that's the orchestrator's call.

## Sub-tasks (OWNS = owner agent / artefact)

| # | Sub-task | OWNS | Output |
|---|----------|------|--------|
| 1 | Write workflow-asserting test script in `tools/__tests__/workflow.test.ts` using `node:assert` + `yaml` parser; runs via `tsx`. Asserts: required steps present, secret name `CSO_CF_TOKEN`, env alias to `CLOUDFLARE_API_TOKEN`, account ID hardcoded as workflow env, no `continue-on-error: true` on deploy, on-push-main trigger, Node 22 setup, wrangler `pages deploy web/dist --project-name brand-atoms --branch main`. | TDD Writer | `tools/__tests__/workflow.test.ts`, `package.json` script `test:workflow` |
| 2 | Implement `.github/workflows/deploy.yml` satisfying the test. Use `cloudflare/wrangler-action@v3` with `command: pages deploy web/dist --project-name brand-atoms --branch main`. Use existing validate.yml as style template. | Coder | `.github/workflows/deploy.yml` |
| 3 | Adversarial review: confirm no test deletion/skip/tautology, no `continue-on-error`/`if: false` swallowing failures, no hardcoded prod values that should be variables, no commented steps. | Tester | PASS/REGRESSION/DECEPTION report |

## Test strategy

- **Static-YAML test** (`tools/__tests__/workflow.test.ts`):
  - Parses `.github/workflows/deploy.yml` with the existing `yaml` dep.
  - Asserts the full step skeleton (by name and `uses`/`run`).
  - Asserts `on.push.branches` contains `main`.
  - Asserts `env.CLOUDFLARE_ACCOUNT_ID` at workflow level == `e1fe0f0ce8ff18da4edc118372c30022`.
  - Asserts deploy step has `env.CLOUDFLARE_API_TOKEN` referencing `secrets.CSO_CF_TOKEN`.
  - Asserts deploy step uses `cloudflare/wrangler-action@v3` with the correct `command` arg.
  - Asserts NO step has `continue-on-error: true`.
  - Asserts a guard step exists (or wrangler-action surfaces the missing-token failure clearly).
- **Run command:** `pnpm test:workflow` (added to root `package.json` scripts).
- **No new runtime dependency** beyond `tsx` + `yaml` (already in repo).

## Risks & mitigations

| Risk | Mitigation |
|------|-----------|
| `wrangler-action` swallows the missing-secret error | Add an explicit guard step before deploy that fails with the required message |
| Cache miss on pnpm slows first run | `actions/setup-node@v4` with `cache: pnpm` (mirrors validate.yml) |
| Converter `dist/` and Astro `web/dist` confusion | Comment in workflow clarifies; build order is root → web |
| Token leak via logs | wrangler-action handles masking; never `echo` the token |

## Definition of done

- All static-YAML tests green via `pnpm test:workflow`.
- Tester returns PASS.
- Commit: `feat(ci): auto-deploy to cloudflare pages (#4)`.
- PR opened with body `Closes #4`. NOT merged.
